import 'package:flutter/material.dart';

class ProfileParentPage extends StatelessWidget {
  const ProfileParentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('profile parent page')),
    );
  }
}