import 'package:flutter/material.dart';

class NotificationParentPage extends StatelessWidget {
  const NotificationParentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('notification')),
    );
  }
}
